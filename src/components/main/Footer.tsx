import { TablerPlus } from "../../lib/Icons";

export default () => {
  return (
    <footer className="subjects-footer">
      <div className="subjects-add-container">
        <button className="button-ell icon-word-button">
          <TablerPlus />
          Add
        </button>
      </div>
    </footer>
  );
}